# Semantic-integrity allowlist for scripts/check-integrity.sh.
#
# Owned by ~/plans/integrity.md. Generated from `scripts/check-integrity.sh
# --list` at the arc's branch point (Jaune b0dc4ee) and then classified by
# hand. Every row is an exact, line-number-independent normalised
# occurrence:
#
#   <rule> <relpath> <source line, trimmed, whitespace runs collapsed>
#
# followed by `## <disposition>`. Two dispositions exist:
#
#   KEEP(wrapper=..., theorem=...)
#       a retained optimized operation, permanently allowed because a named
#       checked/fixed-size wrapper and a named bounds or size theorem
#       discharge it. Both names must be real declarations.
#
#   PENDING(step<N>,<mode>) <why>
#       a known defect this arc removes. `mode` says how it is
#       discharged: `remove` (the operation disappears), `wrapper` (it
#       survives behind a proof-carrying interface and the row becomes a
#       KEEP row naming that interface), `proof-indexed`, `totalize`, or
#       `match`. Every PENDING row is reported on every run.
#
# There is deliberately no R1 row and there never may be: absence of
# `partial def`, `implemented_by`, and `dbg_trace` is asserted outright,
# per ~/plans/silence.md having removed the last of each. The gate refuses
# an allowlist that carries an R1 row at all.
#
# The budget below is the number of PENDING rows. It may only shrink. A
# step that discharges a row deletes it (or rewrites it as KEEP) and
# lowers the budget in the same commit.
#
# pending-budget: 0
#
# At the branch point nothing was discharged: Step 1 inventories, it does
# not repair. The KEEP rows below are Step 8's, and they are all the BLAKE2b
# compression kernel: see the Step-8 note at the end of this header.
#
# ---------------------------------------------------------------------------
# AUDIT NOTE, recorded by Step 1 and binding on Steps 7 and 8.
#
# The Step-1 audit REFUTED the planning-time premise that the optimized crypto
# kernels are `private`. NONE of the bang-operation sites in the closure sits
# inside a `private` declaration. Jaune/Precompiles.lean and Jaune/BLS.lean
# contain zero `private` declarations at all, and Jaune/Hash.lean's only two
# (`rolc`, `round1600`) contain no bang operation. The keccak / SHA-256 /
# RIPEMD-160 / Blake2 kernels are public `Jaune.KECCAK.*`, `Jaune.SHA256.*`,
# and `Jaune.Blake2.*` names reachable by any downstream importer. They are
# internal by convention only.
#
# ~/plans/integrity.md P0.6 fix item 4 permits "a private optimized array
# kernel behind a checked wrapper". Step 8 must therefore either make these
# declarations private or give them fixed-size (`Vector`/structure) inputs. It
# may not merely attach a wrapper and leave the raw kernel exported, because an
# exported raw kernel is exactly the "public helper that currently accepts an
# arbitrary short array" the plan requires to be rejected or totalized.
#
# Not every row below is a live hazard, and the difference drives Step 8's
# effort allocation:
#
#   - `Array.set!` is `Array.setIfInBounds` in Lean core: TOTAL, a silent
#     no-op when out of range. It never panics. The objection to it is
#     allocation cost and an unexpressed invariant, not partiality.
#   - `a[i]!`, `ByteArray.get!`, and `Option.get!` DO panic: they print to
#     stderr and return `default`.
#   - `Jaune.List.slice!` (Jaune/Basic.lean:410) is total despite its name;
#     it zero-pads through `takeD`.
#
# The one genuinely unguarded, reachable, attacker-influenced site the audit
# found is `jumpable` (Jaune/Machine.lean:2393): `cd.get! k` where `k` is a
# popped stack value with no bound relative to `cd.size`. An out-of-range JUMP
# therefore emits a real `panic!` line on stderr and returns `default = 0`;
# the verdict stays correct (0 is not JUMPDEST, so InvalidJumpDestError), but
# the partial read is live on a consensus path. By contrast `noPushBefore`
# (:2384) and `ByteArray.getInst` (:1405) are both guarded by an explicit
# `< size` test, and both `Option.get!` sites (Execution.lean:108 delegation,
# Transaction.lean:1053 system-call error) are guarded by a validity/isSome
# test immediately above. Step 7 owns all of these.
#
# DISCHARGED by Step 7 (2026-07-31): the fakeExpAux fuel panic is gone — the
# loop is a well-founded recursion with recurrence/uniqueness theorems and the
# scripts/check-fake-exp.sh differential oracle; jumpable carries the missing
# bounds test (OOB destination is semantically false); noPushBefore and
# ByteArray.getInst read through their existing tests with proof-indexed
# access; the four simple projections are total matches/head? reads.
#
# DISCHARGED by Step 8 (2026-08-01): all 152 crypto/helper rows are gone or
# rewritten KEEP, and the closure now holds no explicit abort at all.
#
# DISCHARGED by Steps 9-10 (2026-08-01): every R4 stringly-carrier row.
# Step 9 retyped the VM/frame/precompile carrier (EvmError and its family);
# Step 10 retyped the codec, transaction-validation, header/block, fork/config,
# and import producers and made fixture classification a constructor match.
# The pending budget is 0 and R4 now admits no PENDING disposition at all:
# what remains are reviewed KEEP rows -- the named legacy renderer adapters
# (rlpToBlock, stateTransition family, addBlockToChain family, and their two
# shared carrier helpers), guard-only golden probes, and the runner-boundary
# parser lines that read external fixture/CLI input, never a rendered
# diagnostic of this build.
#
#   - Jaune/Hash.lean (91 rows, deleted). `KECCAK.Array.modify!` and its
#     out-of-bounds branch no longer exist. Keccak's state is a
#     `Vector UInt64 25`, SHA-256's schedule a `Vector UInt32 16` under a
#     round budget `n <= 64`, RIPEMD-160's tables `Vector`s with `Fin 16`
#     permutation entries. The reference transcription is polymorphic over the
#     25-lane vector and needs no `Inhabited` fallback; `KECCAK.piln_lt`
#     decides that every pi-table entry names a lane. Ranged
#     `ByteArray.keccak` carries `loc + sz <= bs.size` into the sponge and
#     materialises the zero extension once when it does not hold.
#   - Jaune/EC.lean (4) and Jaune/BLS.lean (6), deleted. Coefficients are read
#     through `List.takeRightV`, which pads on the left to a fixed width. For
#     BN254's `twist` that also CORRECTS the arithmetic on a trimmed
#     coordinate; the three BLS sites were already padding and only gain the
#     proof.
#   - Jaune/Basic.lean (5, deleted). `Bytes.toUInt16` reads through
#     `Bytes.packV`; `ByteArray.sliceD` reads through its own bounds test;
#     `List.splitToArray.aux` carries `idx + sz = array.size` and writes with
#     `Array.set`; `Array.copyD` writes with the total `Array.setIfInBounds`
#     and owes `Array.size_copyD` for the length half of its contract.
#   - Jaune/Precompiles.lean (46 rows -> 43 KEEP). The BLAKE2b kernel is
#     frozen by ~/jaune/scripts/report-integrity-design.md §9 and is NOT
#     reopened: `Blake2.g`/`round`/`rounds` are the reference the equivalence
#     theorems are stated against, and `Blake2.roundVec`/`roundsVec` are the
#     unboxed implementation those theorems cover. What Step 8 added is the
#     checked wrapper the plan requires around them, and made them `private`
#     so the only way in is through it: `bCompress` now REJECTS a
#     chaining value that is not eight words or a message that is not sixteen,
#     rather than padding it, and builds the working vector from
#     length-indexed inputs. `length_readLeUInt64Words` is why the
#     precompile's 213-byte check always satisfies that; `blake2Sigma_size`,
#     `size_blake2Sigma_row`, `size_blake2Sigma_get` and `blake2Sigma_lt`
#     decide the table facts the kernel's indexed reads rest on. Every KEEP
#     row below names that wrapper and one of those theorems.
#
# UPDATE (2026-08-04, ~/plans/keccak-proof.md): keccak's reference-vs-kernel
# equivalence, discussed at length above as an open question, is now proved.
# `Jaune.KECCAK.f1600_eq` shows `f1600` equals `f rndc · UInt64.rol` over the
# retained reference transcription, axioms `[propext, Quot.sound]`. It is a
# statement about the transcription of Jivsov's C code, not FIPS-202
# conformance. This does not add, remove, or reclassify an allowlist row:
# keccak's R2/R3 sections above are still empty and `# pending-budget: 0` is
# unchanged. `scripts/report-integrity-design.md` §"Residual-partiality
# policy" item (5) carries the corrected claim.
# ---------------------------------------------------------------------------


# ---- R2 : Jaune/Hash.lean ----

# ---- R2 : Jaune/Machine.lean ----

# ---- R3 : Jaune/BLS.lean ----

# ---- R3 : Jaune/Basic.lean ----

# ---- R3 : Jaune/EC.lean ----

# ---- R3 : Jaune/Execution.lean ----

# ---- R3 : Jaune/Hash.lean ----

# ---- R3 : Jaune/Machine.lean ----

# ---- R3 : Jaune/Precompiles.lean ----
R3 Jaune/Precompiles.lean (((v.set! a va).set! b vb).set! c vc).set! d vd  ## KEEP(wrapper=bCompress, theorem=Blake2.roundVec_toArray) private Blake2.g/round/rounds -- the retained reference transcription, off the execution path; every read is of a sixteen-word vector supplied by the checked wrapper, and the equivalence theorem is stated against these definitions
R3 Jaune/Precompiles.lean (Blake2.roundVec m (blake2Sigma[r % blake2Sigma.size]!) w)  ## KEEP(wrapper=bCompress, theorem=size_blake2Sigma_get) private Blake2.roundsVec -- the unboxed rounds loop; the selected sigma row has sixteen entries
R3 Jaune/Precompiles.lean (blake2Sigma[r % blake2Sigma.size]!).size = 16 := by  ## KEEP(wrapper=bCompress, theorem=size_blake2Sigma_get) the statement of the bounds theorem itself: it is about the kernel's own read and cannot avoid naming it
R3 Jaune/Precompiles.lean *boxed* elements, so every `Array.set!` in `Blake2.g` is a heap allocation and  ## KEEP(wrapper=bCompress, theorem=Blake2.roundVec_toArray) private Blake2.g/round/rounds -- the retained reference transcription, off the execution path; every read is of a sixteen-word vector supplied by the checked wrapper, and the equivalence theorem is stated against these definitions
R3 Jaune/Precompiles.lean -- Blake2.g v 0 4 8 12 (m[s[0]!]!) (m[s[1]!]!)  ## KEEP(wrapper=bCompress, theorem=size_blake2Sigma_get) private Blake2.roundVec -- the unboxed compression round; bCompress rejects any message that is not sixteen words, blake2Sigma_lt bounds every sigma entry by 16, and size_blake2Sigma_get gives the row its width
R3 Jaune/Precompiles.lean -- Blake2.g v 0 5 10 15 (m[s[8]!]!) (m[s[9]!]!)  ## KEEP(wrapper=bCompress, theorem=size_blake2Sigma_get) private Blake2.roundVec -- the unboxed compression round; bCompress rejects any message that is not sixteen words, blake2Sigma_lt bounds every sigma entry by 16, and size_blake2Sigma_get gives the row its width
R3 Jaune/Precompiles.lean -- Blake2.g v 1 5 9 13 (m[s[2]!]!) (m[s[3]!]!)  ## KEEP(wrapper=bCompress, theorem=size_blake2Sigma_get) private Blake2.roundVec -- the unboxed compression round; bCompress rejects any message that is not sixteen words, blake2Sigma_lt bounds every sigma entry by 16, and size_blake2Sigma_get gives the row its width
R3 Jaune/Precompiles.lean -- Blake2.g v 1 6 11 12 (m[s[10]!]!) (m[s[11]!]!)  ## KEEP(wrapper=bCompress, theorem=size_blake2Sigma_get) private Blake2.roundVec -- the unboxed compression round; bCompress rejects any message that is not sixteen words, blake2Sigma_lt bounds every sigma entry by 16, and size_blake2Sigma_get gives the row its width
R3 Jaune/Precompiles.lean -- Blake2.g v 2 6 10 14 (m[s[4]!]!) (m[s[5]!]!)  ## KEEP(wrapper=bCompress, theorem=size_blake2Sigma_get) private Blake2.roundVec -- the unboxed compression round; bCompress rejects any message that is not sixteen words, blake2Sigma_lt bounds every sigma entry by 16, and size_blake2Sigma_get gives the row its width
R3 Jaune/Precompiles.lean -- Blake2.g v 2 7 8 13 (m[s[12]!]!) (m[s[13]!]!)  ## KEEP(wrapper=bCompress, theorem=size_blake2Sigma_get) private Blake2.roundVec -- the unboxed compression round; bCompress rejects any message that is not sixteen words, blake2Sigma_lt bounds every sigma entry by 16, and size_blake2Sigma_get gives the row its width
R3 Jaune/Precompiles.lean -- Blake2.g v 3 4 9 14 (m[s[14]!]!) (m[s[15]!]!)  ## KEEP(wrapper=bCompress, theorem=size_blake2Sigma_get) private Blake2.roundVec -- the unboxed compression round; bCompress rejects any message that is not sixteen words, blake2Sigma_lt bounds every sigma entry by 16, and size_blake2Sigma_get gives the row its width
R3 Jaune/Precompiles.lean -- Blake2.g v 3 7 11 15 (m[s[6]!]!) (m[s[7]!]!)  ## KEEP(wrapper=bCompress, theorem=size_blake2Sigma_get) private Blake2.roundVec -- the unboxed compression round; bCompress rejects any message that is not sixteen words, blake2Sigma_lt bounds every sigma entry by 16, and size_blake2Sigma_get gives the row its width
R3 Jaune/Precompiles.lean -- written back once: the intermediate `set!`/`get!` round trips of the  ## KEEP(wrapper=bCompress, theorem=Blake2.roundVec_toArray) private Blake2.g/round/rounds -- the retained reference transcription, off the execution path; every read is of a sixteen-word vector supplied by the checked wrapper, and the equivalence theorem is stated against these definitions
R3 Jaune/Precompiles.lean Blake2.g v 3 4 9 14 (m[s[14]!]!) (m[s[15]!]!)  ## KEEP(wrapper=bCompress, theorem=Blake2.roundVec_toArray) private Blake2.g/round/rounds -- the retained reference transcription, off the execution path; every read is of a sixteen-word vector supplied by the checked wrapper, and the equivalence theorem is stated against these definitions
R3 Jaune/Precompiles.lean Blake2.rounds m k n (Blake2.round m (blake2Sigma[r % blake2Sigma.size]!) v)  ## KEEP(wrapper=bCompress, theorem=Blake2.roundsVec_toArray) private Blake2.rounds -- reference rounds loop, off the execution path; the sigma row it selects has sixteen entries by size_blake2Sigma_get
R3 Jaune/Precompiles.lean let v := Blake2.g v 0 4 8 12 (m[s[0]!]!) (m[s[1]!]!)  ## KEEP(wrapper=bCompress, theorem=Blake2.roundVec_toArray) private Blake2.g/round/rounds -- the retained reference transcription, off the execution path; every read is of a sixteen-word vector supplied by the checked wrapper, and the equivalence theorem is stated against these definitions
R3 Jaune/Precompiles.lean let v := Blake2.g v 0 5 10 15 (m[s[8]!]!) (m[s[9]!]!)  ## KEEP(wrapper=bCompress, theorem=Blake2.roundVec_toArray) private Blake2.g/round/rounds -- the retained reference transcription, off the execution path; every read is of a sixteen-word vector supplied by the checked wrapper, and the equivalence theorem is stated against these definitions
R3 Jaune/Precompiles.lean let v := Blake2.g v 1 5 9 13 (m[s[2]!]!) (m[s[3]!]!)  ## KEEP(wrapper=bCompress, theorem=Blake2.roundVec_toArray) private Blake2.g/round/rounds -- the retained reference transcription, off the execution path; every read is of a sixteen-word vector supplied by the checked wrapper, and the equivalence theorem is stated against these definitions
R3 Jaune/Precompiles.lean let v := Blake2.g v 1 6 11 12 (m[s[10]!]!) (m[s[11]!]!)  ## KEEP(wrapper=bCompress, theorem=Blake2.roundVec_toArray) private Blake2.g/round/rounds -- the retained reference transcription, off the execution path; every read is of a sixteen-word vector supplied by the checked wrapper, and the equivalence theorem is stated against these definitions
R3 Jaune/Precompiles.lean let v := Blake2.g v 2 6 10 14 (m[s[4]!]!) (m[s[5]!]!)  ## KEEP(wrapper=bCompress, theorem=Blake2.roundVec_toArray) private Blake2.g/round/rounds -- the retained reference transcription, off the execution path; every read is of a sixteen-word vector supplied by the checked wrapper, and the equivalence theorem is stated against these definitions
R3 Jaune/Precompiles.lean let v := Blake2.g v 2 7 8 13 (m[s[12]!]!) (m[s[13]!]!)  ## KEEP(wrapper=bCompress, theorem=Blake2.roundVec_toArray) private Blake2.g/round/rounds -- the retained reference transcription, off the execution path; every read is of a sixteen-word vector supplied by the checked wrapper, and the equivalence theorem is stated against these definitions
R3 Jaune/Precompiles.lean let v := Blake2.g v 3 7 11 15 (m[s[6]!]!) (m[s[7]!]!)  ## KEEP(wrapper=bCompress, theorem=Blake2.roundVec_toArray) private Blake2.g/round/rounds -- the retained reference transcription, off the execution path; every read is of a sixteen-word vector supplied by the checked wrapper, and the equivalence theorem is stated against these definitions
R3 Jaune/Precompiles.lean let va : UInt64 := v[a]!  ## KEEP(wrapper=bCompress, theorem=Blake2.roundVec_toArray) private Blake2.g/round/rounds -- the retained reference transcription, off the execution path; every read is of a sixteen-word vector supplied by the checked wrapper, and the equivalence theorem is stated against these definitions
R3 Jaune/Precompiles.lean let vb : UInt64 := v[b]!  ## KEEP(wrapper=bCompress, theorem=Blake2.roundVec_toArray) private Blake2.g/round/rounds -- the retained reference transcription, off the execution path; every read is of a sixteen-word vector supplied by the checked wrapper, and the equivalence theorem is stated against these definitions
R3 Jaune/Precompiles.lean let vc : UInt64 := v[c]!  ## KEEP(wrapper=bCompress, theorem=Blake2.roundVec_toArray) private Blake2.g/round/rounds -- the retained reference transcription, off the execution path; every read is of a sixteen-word vector supplied by the checked wrapper, and the equivalence theorem is stated against these definitions
R3 Jaune/Precompiles.lean let vd : UInt64 := v[d]!  ## KEEP(wrapper=bCompress, theorem=Blake2.roundVec_toArray) private Blake2.g/round/rounds -- the retained reference transcription, off the execution path; every read is of a sixteen-word vector supplied by the checked wrapper, and the equivalence theorem is stated against these definitions
R3 Jaune/Precompiles.lean let x := m[s[0]!]!  ## KEEP(wrapper=bCompress, theorem=size_blake2Sigma_get) private Blake2.roundVec -- the unboxed compression round; bCompress rejects any message that is not sixteen words, blake2Sigma_lt bounds every sigma entry by 16, and size_blake2Sigma_get gives the row its width
R3 Jaune/Precompiles.lean let x := m[s[10]!]!  ## KEEP(wrapper=bCompress, theorem=size_blake2Sigma_get) private Blake2.roundVec -- the unboxed compression round; bCompress rejects any message that is not sixteen words, blake2Sigma_lt bounds every sigma entry by 16, and size_blake2Sigma_get gives the row its width
R3 Jaune/Precompiles.lean let x := m[s[12]!]!  ## KEEP(wrapper=bCompress, theorem=size_blake2Sigma_get) private Blake2.roundVec -- the unboxed compression round; bCompress rejects any message that is not sixteen words, blake2Sigma_lt bounds every sigma entry by 16, and size_blake2Sigma_get gives the row its width
R3 Jaune/Precompiles.lean let x := m[s[14]!]!  ## KEEP(wrapper=bCompress, theorem=size_blake2Sigma_get) private Blake2.roundVec -- the unboxed compression round; bCompress rejects any message that is not sixteen words, blake2Sigma_lt bounds every sigma entry by 16, and size_blake2Sigma_get gives the row its width
R3 Jaune/Precompiles.lean let x := m[s[2]!]!  ## KEEP(wrapper=bCompress, theorem=size_blake2Sigma_get) private Blake2.roundVec -- the unboxed compression round; bCompress rejects any message that is not sixteen words, blake2Sigma_lt bounds every sigma entry by 16, and size_blake2Sigma_get gives the row its width
R3 Jaune/Precompiles.lean let x := m[s[4]!]!  ## KEEP(wrapper=bCompress, theorem=size_blake2Sigma_get) private Blake2.roundVec -- the unboxed compression round; bCompress rejects any message that is not sixteen words, blake2Sigma_lt bounds every sigma entry by 16, and size_blake2Sigma_get gives the row its width
R3 Jaune/Precompiles.lean let x := m[s[6]!]!  ## KEEP(wrapper=bCompress, theorem=size_blake2Sigma_get) private Blake2.roundVec -- the unboxed compression round; bCompress rejects any message that is not sixteen words, blake2Sigma_lt bounds every sigma entry by 16, and size_blake2Sigma_get gives the row its width
R3 Jaune/Precompiles.lean let x := m[s[8]!]!  ## KEEP(wrapper=bCompress, theorem=size_blake2Sigma_get) private Blake2.roundVec -- the unboxed compression round; bCompress rejects any message that is not sixteen words, blake2Sigma_lt bounds every sigma entry by 16, and size_blake2Sigma_get gives the row its width
R3 Jaune/Precompiles.lean let y := m[s[11]!]!  ## KEEP(wrapper=bCompress, theorem=size_blake2Sigma_get) private Blake2.roundVec -- the unboxed compression round; bCompress rejects any message that is not sixteen words, blake2Sigma_lt bounds every sigma entry by 16, and size_blake2Sigma_get gives the row its width
R3 Jaune/Precompiles.lean let y := m[s[13]!]!  ## KEEP(wrapper=bCompress, theorem=size_blake2Sigma_get) private Blake2.roundVec -- the unboxed compression round; bCompress rejects any message that is not sixteen words, blake2Sigma_lt bounds every sigma entry by 16, and size_blake2Sigma_get gives the row its width
R3 Jaune/Precompiles.lean let y := m[s[15]!]!  ## KEEP(wrapper=bCompress, theorem=size_blake2Sigma_get) private Blake2.roundVec -- the unboxed compression round; bCompress rejects any message that is not sixteen words, blake2Sigma_lt bounds every sigma entry by 16, and size_blake2Sigma_get gives the row its width
R3 Jaune/Precompiles.lean let y := m[s[1]!]!  ## KEEP(wrapper=bCompress, theorem=size_blake2Sigma_get) private Blake2.roundVec -- the unboxed compression round; bCompress rejects any message that is not sixteen words, blake2Sigma_lt bounds every sigma entry by 16, and size_blake2Sigma_get gives the row its width
R3 Jaune/Precompiles.lean let y := m[s[3]!]!  ## KEEP(wrapper=bCompress, theorem=size_blake2Sigma_get) private Blake2.roundVec -- the unboxed compression round; bCompress rejects any message that is not sixteen words, blake2Sigma_lt bounds every sigma entry by 16, and size_blake2Sigma_get gives the row its width
R3 Jaune/Precompiles.lean let y := m[s[5]!]!  ## KEEP(wrapper=bCompress, theorem=size_blake2Sigma_get) private Blake2.roundVec -- the unboxed compression round; bCompress rejects any message that is not sixteen words, blake2Sigma_lt bounds every sigma entry by 16, and size_blake2Sigma_get gives the row its width
R3 Jaune/Precompiles.lean let y := m[s[7]!]!  ## KEEP(wrapper=bCompress, theorem=size_blake2Sigma_get) private Blake2.roundVec -- the unboxed compression round; bCompress rejects any message that is not sixteen words, blake2Sigma_lt bounds every sigma entry by 16, and size_blake2Sigma_get gives the row its width
R3 Jaune/Precompiles.lean let y := m[s[9]!]!  ## KEEP(wrapper=bCompress, theorem=size_blake2Sigma_get) private Blake2.roundVec -- the unboxed compression round; bCompress rejects any message that is not sixteen words, blake2Sigma_lt bounds every sigma entry by 16, and size_blake2Sigma_get gives the row its width
R3 Jaune/Precompiles.lean out of arrays — a read does not allocate, only `set!` does. The mix temporary  ## KEEP(wrapper=bCompress, theorem=Blake2.roundVec_toArray) private Blake2.g/round/rounds -- the retained reference transcription, off the execution path; every read is of a sixteen-word vector supplied by the checked wrapper, and the equivalence theorem is stated against these definitions

# ---- R3 : Jaune/Transaction.lean ----

# ---- R4 : Jaune/BLS.lean ----

# ---- R4 : Jaune/Basic.lean ----

# ---- R4 : Jaune/Execution.lean ----

# ---- R4 : Jaune/Fork.lean ----

# ---- R4 : Jaune/Machine.lean ----

# ---- R4 : Jaune/Precompiles.lean ----

# ---- R4 : Jaune/Sufficiency.lean ----

# ---- R4 : Jaune/Transaction.lean ----
R4 Jaune/Transaction.lean (blockRlp : Bytes) : Except String (BlockChain ⊕ String) :=  ## KEEP(adapter=addBlockToChainWithE/addBlockToChainUsingE, bridge=addBlockToChainWith_eq_ok_inl) Step-10 legacy renderer adapters: ImportOutcome.renderLegacy over the typed import cores; retained because Blanc's protected addBlockToChain_preserves_solvent is their Prague instance
R4 Jaune/Transaction.lean Except String (BlockChain ⊕ String)  ## KEEP(adapter=ImportOutcome.renderLegacy, bridge=addBlockToChainCanonical_eq_ok_inl) the one renderer collapsing a typed import result onto the legacy channels; every legacy import adapter goes through it
R4 Jaune/Transaction.lean private def errorText? {α : Type} : Except String α → Option String  ## KEEP(guard-only) golden-text probe reading a legacy adapter's rendered error for #guard renderer goldens; classification never reads it
R4 Jaune/Transaction.lean def rlpToBlock (rlp : Bytes) : Except String (Block × B256) :=  ## KEEP(adapter=rlpToBlockE, bridge=rlpToBlock_eq_ok_iff) Step-10 legacy renderer adapter: byte-identical mapError DecodeError.render over the typed core; retained by name and type because Blanc's protected addBlockToChain_preserves_solvent quantifies over this equation
R4 Jaune/Transaction.lean Except String (BlockChain ⊕ String) :=  ## KEEP(adapter=addBlockToChainCanonicalE/addBlockToChainAtE + addBlockToChain, bridge=addBlockToChainCanonical_eq_ok_inl) Step-10 legacy renderer adapters (addBlockToChainCanonical/At/addBlockToChain): ImportOutcome.renderLegacy over the typed import cores; addBlockToChain is the name Blanc's protected theorem states
R4 Jaune/Transaction.lean Except String BlockChain :=  ## KEEP(adapter=stateTransitionE, bridge=stateTransitionWith_eq_ok_iff) Step-10 legacy renderer adapters (stateTransitionWith/stateTransition): mapError TransitionError.render over the typed core; the names Blanc's protected theorems and the frozen rfl identities state
R4 Jaune/Transaction.lean Except String BlockChain := do  ## KEEP(adapter=stateTransitionE, bridge=stateTransitionWith_eq_ok_iff) Step-10 legacy renderer adapters (stateTransitionAt/stateTransitionUsing): fork/schedule selection composed over the stateTransitionWith adapter; byte-identical renders

# ---- R4 : Jaune/FixtureException.lean ----
R4 Jaune/FixtureException.lean def parseExpectation (s : String) : Except String (List FixtureException) := do  ## KEEP(parser=expectException) the one external-string parser: reads the fixture's own expectException label, never a rendered diagnostic of this build

# ---- R4 : Main.lean ----
R4 Main.lean def Except.toIO {ξ : Type} : Except String ξ → IO ξ  ## KEEP(boundary=CLI) lifts an already-rendered harness error into IO; rendering happened at a typed boundary
R4 Main.lean def Lean.Json.toString? : Lean.Json → Option String  ## KEEP(parser=JSON) fixture-JSON field accessor; not an error channel
R4 Main.lean def Option.remove0x (s : String) : Option String :=  ## KEEP(parser=JSON) fixture-JSON hex-prefix syntax; not an error channel
R4 Main.lean def blockchainTestSibling? (path : String) : Option String :=  ## KEEP(boundary=CLI) rewrites a wrong-tree fixture path to its blockchain_tests sibling for the refusal message; a filesystem path carrying no verdict, read by nothing but the diagnostic
R4 Main.lean def fixtureCaseFormat? : (Nat × String × Lean.Json) → Option String  ## KEEP(parser=JSON) fixture-JSON field accessor reading EEST's own _info.fixture-format label; not an error channel, and the branch it drives refuses a wrong-format file rather than classifying one
R4 Main.lean def getNetwork : List String → Option String  ## KEEP(parser=CLI) command-line option accessor; not an error channel
R4 Main.lean def getTxExMap (j : Lean.Json) : IO (Option String × Bytes) := do  ## KEEP(parser=expectException) carries the fixture's raw expectException label to parseExpectation; not an error channel
